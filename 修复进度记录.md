一、问题现状
- iOS 在开启“智能模式”或“全局模式”后，VPN 显示已连接，但整机无网络
- 当前问题仍未彻底修复，最新真机反馈仍为“还是一样”
- 最新 `Tunnel` 日志已确认：启动、下发网络设置、安装 bridge、核心启动、`readPackets`、`MobileFeedPacketBytes` 都在持续发生
- 当前排查重点已从基础配置项微调，进一步收敛到“mihomo 收到包后为何没有回写 `writePackets`”

二、已确认结论
- [x] 当前 `mihomo-clash/ios/mihomo` 的 iOS 实现不是 `tun.file-descriptor` 直连模式
- [x] 当前 iOS 实现依赖 Swift 层通过 `packetFlow.readPackets` / `packetFlow.writePackets` 做数据中转
- [x] Go/mihomo 在这里主要承担流量处理，不直接读写 iOS `utun fd`
- [x] `mobile.go` 会对 iOS 配置做收敛处理，`tun.file-descriptor` 和注入的 `dns-hijack` 不能作为最终有效依据
- [x] 仅依靠调低 MTU、关闭 IPv6 默认路由、调整 TUN 地址，仍未解决整机断网

三、已完成修复与排查记录

1. 基础网络参数收敛
- [x] 调整 iOS TUN 地址为 `172.19.0.1/30`
- [x] 下调 MTU 到 `1400`
- [x] 禁用 IPv6 默认路由
- [x] 仅保留 IPv4 DNS / IPv4 劫持方向
- 相关提交：
  - `005cf97` Fix iOS VPN server metadata
  - `6e3d9fc` Adjust iOS tunnel addresses
  - `bb3e325` Lower iOS tunnel MTU
  - `d0f68be` Disable iOS IPv6 tunnel routing

2. PacketFlow 接入方式修正
- [x] 曾尝试参考其他项目走 `fd` 路径，后续已确认与当前 mihomo iOS 源码不匹配
- [x] 已恢复为正确的 Swift PacketFlow 中转模型
- [x] 保留 `packetFlow.readPackets -> MobileFeedPacketBytes -> bridge.writePacket -> packetFlow.writePackets` 链路
- [x] 移除错误的 `tun.file-descriptor` 注入思路
- 相关提交：
  - `90e695e` Fix iOS packet tunnel packet flow integration

3. DNS 接管方向修正
- [x] 将 iOS 系统 DNS 从公网 DNS 改为 TUN 内部 DNS 地址 `172.19.0.2`
- [x] 注入 `inet4-address: 172.19.0.1/30`
- [x] 尝试让系统 DNS 与 mihomo iOS 侧期望的内部 DNS 地址对齐
- 结果：真机测试后仍然“整机无网络”
- 相关提交：
  - `d2f6fe8` Fix iOS tunnel DNS handoff

4. 运行时埋点与日志读取能力
- [x] 在 `PacketTunnelProvider.swift` 中增加启动、读包、喂包、写包、网络切换等调试日志
- [x] 日志同时写入 App Group 文件 `packet_tunnel_debug.log`
- [x] 增加宿主与拓展之间的 `getTunnelDebugLog` 获取能力
- [x] 新增 iOS 专用 Flutter 页面查看 Tunnel 日志，方便真机直接复制日志
- 相关提交：
  - `f0abbaf` Add iOS packet tunnel telemetry
  - `1e7aa6a` Add iOS tunnel debug log viewer

5. 最新真机日志结论（`2026-04-01`）
- [x] 日志显示 `start requested -> network settings applied -> packet flow bridge installed -> core started` 启动链路完整
- [x] 日志显示 `read packets total=600`，说明 `packetFlow.readPackets` 在持续从系统 TUN 侧收到流量
- [x] 日志显示 `feed packet success total=600`，且没有出现 `feed packet failed`，说明 Swift -> `MobileFeedPacketBytes` 入队链路正常
- [x] 当前日志中完全没有 `write packet` 记录，说明 `bridge.writePacket -> packetFlow.writePackets` 这一侧尚未产生任何可回写数据
- [x] 结合现象，高度怀疑是 iOS 14+ Network Extension 沙盒权限坑：即没有显式初始化 `NWPathMonitor`，导致 NE 进程被系统剥夺外网访问权限
- [x] 这会导致：mihomo 收到系统进来的包 -> 尝试通过 TCP/UDP 出站连接代理节点或解析外部 DNS -> 被 iOS 系统直接拦截 -> 没有任何回包 -> 整机无网

6. Network Extension 权限激活修复
- [x] 在 `PacketTunnelProvider.swift` 的 `init()` 中增加 `let _ = NWPathMonitor()` 强制触发网络权限申请
- 相关提交：
  - 待提交：Fix iOS network extension network permission by initializing NWPathMonitor

四、当前代码侧状态
- iOS 原生日志查看链路已具备：
  - `PacketTunnelProvider.swift` 已写入调试日志
  - `AppDelegate.swift` 已暴露 `getTunnelDebugLog`
  - Flutter 侧已新增 iOS Tunnel 日志页面
- 当前可直接通过 App 内调试入口查看并复制 Tunnel 日志，无需依赖 Mac Console

五、当前判断
- 主因高度指向 iOS Network Extension 的网络访问权限问题
- 现有日志已经确认：
  - `packetFlow.readPackets` 可持续读到包
  - `MobileFeedPacketBytes` 可持续成功入队
  - 当前卡点位于 `MobileFeedPacketBytes` 之后、`bridge.writePacket / packetFlow.writePackets` 之前
- 这意味着当前“整机无网”是因为核心侧在沙盒内没有网络权限（没有主动初始化 `NWPathMonitor`），导致出站全部被系统拦截，无法产出任何返回流量
- 若继续只调整 iOS TUN 参数，命中问题根因的概率已经很低
- 下一阶段需验证加上 `NWPathMonitor()` 后，mihomo 是否能正常出站并触发回写

六、下一步计划
- [ ] 提交并安装包含 `NWPathMonitor` 修复的最新 iOS 包
- [ ] 真机再次复现并抓取 Tunnel 日志
- [ ] 检查日志中是否出现 `write packet count=...`
- [ ] 若仍无回包，再考虑在 mihomo iOS 侧补充更靠近核心的数据面日志（DNS 处理、出站建连等）

七、备注
- `flutter test` 当前通过
- `flutter analyze` 仍会被仓库内 `clashmi-main` 子目录的历史错误拖红，这不是本轮 iOS 修复新增问题
